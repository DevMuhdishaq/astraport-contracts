[package]
name = "astraport-events"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]

[dependencies]
soroban-sdk = { workspace = true, features = ["contract", "std"] }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["contract", "std"] }
